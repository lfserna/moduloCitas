const themeToggle=document.getElementById('themeToggle');if(themeToggle){if(localStorage.getItem('theme')==='dark')document.body.classList.add('dark');themeToggle.onclick=()=>{document.body.classList.toggle('dark');localStorage.setItem('theme',document.body.classList.contains('dark')?'dark':'light')}}
const form=document.getElementById('appointmentForm');if(form){let step=1,total=5,specialty=null,professional=null,schedule=null,schedules=[],cal=new Date();const q=id=>document.getElementById(id);const stepNames=['Especialidad','Profesional','Fecha y hora','Datos','Confirmación'];function show(n){step=n;document.querySelectorAll('.step').forEach(s=>s.classList.toggle('is-active',Number(s.dataset.step)===step));q('stepText').textContent=`Paso ${step}/${total}`;q('stepName').textContent=stepNames[step-1];q('progressBar').style.width=`${step/total*100}%`;q('prevStep').style.visibility=step===1?'hidden':'visible';q('nextStep').classList.toggle('is-hidden',step===total);q('submitBtn').classList.toggle('is-hidden',step!==total);if(step===5)summary()}function valid(){if(step===1&&!specialty){window.alert('Selecciona una especialidad.');return false}if(step===2&&!professional){window.alert('Selecciona un profesional.');return false}if(step===3&&!schedule){window.alert('Selecciona fecha y hora.');return false}if(step===4&&(!q('nombreCompleto').value.trim()||!q('telefono').value.trim()||!q('email').value.trim())){window.alert('Completa nombre, teléfono y correo.');return false}return true}
document.querySelectorAll('[data-specialty-id]').forEach(b=>{b.onclick=async()=>{document.querySelectorAll('[data-specialty-id]').forEach(x=>x.classList.remove('is-selected'));b.classList.add('is-selected');specialty={id:b.dataset.specialtyId,name:b.dataset.specialtyName};q('especialidadId').value=specialty.id;professional=null;schedule=null;q('profesionalId').value='';q('horarioId').value='';await professionals(specialty.id)}});async function professionals(id){const box=q('professionalsList');box.innerHTML='<p class="muted">Cargando...</p>';const list=await(await fetch(`/datos/profesionales/${id}`)).json();box.innerHTML='';list.forEach(p=>{const b=document.createElement('button');b.type='button';b.className='option-card';b.innerHTML=`<strong>${p.nombre}</strong><span>${p.descripcion||'Profesional disponible'}</span><span>Matrícula: ${p.matricula||'Demo'}</span>`;b.onclick=async()=>{box.querySelectorAll('.option-card').forEach(x=>x.classList.remove('is-selected'));b.classList.add('is-selected');professional=p;q('profesionalId').value=p.id;schedule=null;q('horarioId').value='';await loadSchedules(p.id)};box.appendChild(b)});if(!list.length)box.innerHTML='<p class="muted">No hay profesionales registrados.</p>'}async function loadSchedules(id){schedules=await(await fetch(`/datos/horarios/${id}`)).json();const first=schedules.find(x=>x.disponible)?.fecha;cal=first?new Date(first+'T12:00:00'):new Date();calendar();times(null)}function calendar(){const grid=q('calendarGrid');grid.innerHTML='';const y=cal.getFullYear(),m=cal.getMonth(),first=new Date(y,m,1),last=new Date(y,m+1,0);q('calendarTitle').textContent=first.toLocaleDateString('es-BO',{month:'long',year:'numeric'});let offset=(first.getDay()+6)%7;for(let i=0;i<offset;i++){let e=document.createElement('button');e.type='button';e.className='day-btn is-muted';grid.appendChild(e)}for(let d=1;d<=last.getDate();d++){let date=new Date(y,m,d),iso=date.toISOString().slice(0,10),daySchedules=schedules.filter(x=>x.fecha===iso),has=daySchedules.some(x=>x.disponible),b=document.createElement('button');b.type='button';b.className='day-btn '+(has?'has-times':'');b.textContent=d;b.disabled=!daySchedules.length;b.onclick=()=>{grid.querySelectorAll('.day-btn').forEach(x=>x.classList.remove('is-selected'));b.classList.add('is-selected');times(iso)};grid.appendChild(b)}}function times(iso){const box=q('timeList');if(!iso){box.innerHTML='<p class="muted">Selecciona un día disponible.</p>';return}box.innerHTML='';schedules.filter(x=>x.fecha===iso).forEach(item=>{const b=document.createElement('button');b.type='button';b.className='time-btn';b.textContent=`${item.hora_inicio} - ${item.hora_fin}`;b.disabled=!item.disponible;b.onclick=()=>{box.querySelectorAll('.time-btn').forEach(x=>x.classList.remove('is-selected'));b.classList.add('is-selected');schedule=item;q('horarioId').value=item.id};box.appendChild(b)})}function summary(){q('summaryCard').innerHTML=`<p><strong>Especialidad:</strong> ${specialty?.name||'-'}</p><p><strong>Profesional:</strong> ${professional?.nombre||'-'}</p><p><strong>Fecha:</strong> ${schedule?.fecha||'-'}</p><p><strong>Hora:</strong> ${schedule?`${schedule.hora_inicio} - ${schedule.hora_fin}`:'-'}</p><p><strong>Paciente:</strong> ${q('nombreCompleto').value||'-'}</p><p><strong>Contacto:</strong> ${q('telefono').value||'-'} / ${q('email').value||'-'}</p>`}q('prevMonth').onclick=()=>{cal.setMonth(cal.getMonth()-1);calendar()};q('nextMonth').onclick=()=>{cal.setMonth(cal.getMonth()+1);calendar()};q('prevStep').onclick=()=>show(Math.max(1,step-1));q('nextStep').onclick=()=>{if(valid())show(Math.min(total,step+1))};form.onsubmit=e=>{if(!valid())e.preventDefault()};show(1)}
